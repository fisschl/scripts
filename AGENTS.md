## 编程规范

必须为 Rust 代码编写完备的中文注释，且符合 Rust 注释规范。

在编码完成后，执行 `cargo check`，`cargo clippy`，`cargo fmt`，确保修复所有问题。

## 项目结构

在 `src/bin` 模块中，存放每个脚本的 Rust 文件。每个文件都是独立的，可单独编译的工具。工具的文件名最能够概括该工具的功能。

在 `src/utils` 模块中，存放通用性较强，可以被复用的函数。如果工具需要某个功能，不妨先检查 `utils` 模块中是否存在相关函数。
